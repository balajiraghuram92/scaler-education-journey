version: '3.8'

# Local Development Override - Enables local docker build context
services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile

  app:
    build:
      context: app
      dockerfile: Dockerfile
